# 1. Load the Devbox tools (Python, Poetry, etc.)
eval "$(devbox shellenv)"

# 2. Load your secrets from .env
dotenv

# 3. Path to your Poetry virtualenv
export VIRTUAL_ENV="./.venv"
export PATH="$VIRTUAL_ENV/bin:$PATH"